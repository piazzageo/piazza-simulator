package main

import (
	"log"
	//	"github.com/mpgerlek/piazza-simulator/piazza"
)

func Gateway(registryHost string) {
	log.Printf("gateway started at registry host %v\n", registryHost)

	// register with service

	// handle POSTs, by posting to Dispatcher queue
}
